input {
    file {
        type => "nomination:msmq:uploads"
        tags => [ "nomination.msmq.logs" ]
        path => "/\/\GB-SLO-SVV-0676/\logs/\dh.Messaging/\Nominations/\Uploads.log*"
        add_field => { 
            "server" => "systest" 
            "market" => "tescouk" 
        }
        codec => multiline {
            # Grok pattern names are valid! :)
            pattern => "^%{YEAR}-%{MONTHNUM}-%{MONTHDAY} %{TIME},%{INT} \[%{INT:Thread}\] %{WORD:LogType}"
            negate => true
            what => previous
        }
    }
    file {
        type => "nomination:msmq:create"
        tags => [ "nomination.msmq.logs" ]
        path => "/\/\GB-SLO-SVV-0676/\logs/\dh.Messaging/\Nominations/\Creater.log*"
        add_field => { 
            "server" => "systest" 
            "market" => "tescouk" 
        }
        codec => multiline {
            # Grok pattern names are valid! :)
            pattern => "^%{YEAR}-%{MONTHNUM}-%{MONTHDAY} %{TIME},%{INT} \[%{INT:Thread}\] %{WORD:LogType}"
            negate => true
            what => previous
        }
    }
    file {
        type => "nomination:msmq:update"
        tags => [ "nomination.msmq.logs" ]
        path => "/\/\GB-SLO-SVV-0676/\logs/\dh.Messaging/\Nominations/\Updater.log*"
        add_field => { 
            "server" => "systest" 
            "market" => "tescouk" 
        }
        codec => multiline {
            # Grok pattern names are valid! :)
            pattern => "^%{YEAR}-%{MONTHNUM}-%{MONTHDAY} %{TIME},%{INT} \[%{INT:Thread}\] %{WORD:LogType}"
            negate => true
            what => previous
        }
    }
    file {
        type => "nomination:msmq:group"
        tags => [ "nomination.msmq.logs" ]
        path => "/\/\GB-SLO-SVV-0676/\logs/\dh.Messaging/\Nominations/\Group.log*"
        add_field => { 
            "server" => "systest" 
            "market" => "tescouk" 
        }
        codec => multiline {
            # Grok pattern names are valid! :)
            pattern => "^%{YEAR}-%{MONTHNUM}-%{MONTHDAY} %{TIME},%{INT} \[%{INT:Thread}\] %{WORD:LogType}"
            negate => true
            what => previous
        }
    }
    file {
        type => "nomination:msmq:emailer"
        tags => [ "nomination.msmq.logs" ]
        path => "/\/\GB-SLO-SVV-0676/\logs/\dh.Messaging/\Nominations/\Emailer.log*"
        add_field => { 
            "server" => "systest" 
            "market" => "tescouk" 
        }
        codec => multiline {
            # Grok pattern names are valid! :)
            pattern => "^%{YEAR}-%{MONTHNUM}-%{MONTHDAY} %{TIME},%{INT} \[%{INT:Thread}\] %{WORD:LogType}"
            negate => true
            what => previous
        }
    }
    file {
        type => "nomination:msmq:uploads"
        tags => [ "nomination.msmq.logs" ]
        path => "/\/\US-COL-SVV-0598/\c$/\logs/\dh.Messaging/\Nominations/\Uploads.log*"
        add_field => { 
            "server" => "systest" 
            "market" => "krogerus" 
        }
        codec => multiline {
            # Grok pattern names are valid! :)
            pattern => "^%{YEAR}-%{MONTHNUM}-%{MONTHDAY} %{TIME},%{INT} \[%{INT:Thread}\] %{WORD:LogType}"
            negate => true
            what => previous
        }
    }
    file {
        type => "nomination:msmq:create"
        tags => [ "nomination.msmq.logs" ]
        path => "/\/\US-COL-SVV-0598/\c$/\logs/\dh.Messaging/\Nominations/\Creater.log*"
        add_field => { 
            "server" => "systest" 
            "market" => "krogerus" 
        }
        codec => multiline {
            # Grok pattern names are valid! :)
            pattern => "^%{YEAR}-%{MONTHNUM}-%{MONTHDAY} %{TIME},%{INT} \[%{INT:Thread}\] %{WORD:LogType}"
            negate => true
            what => previous
        }
    }
    file {
        type => "nomination:msmq:update"
        tags => [ "nomination.msmq.logs" ]
        path => "/\/\US-COL-SVV-0598/\c$/\logs/\dh.Messaging/\Nominations/\Updater.log*"
        add_field => { 
            "server" => "systest" 
            "market" => "krogerus" 
        }
        codec => multiline {
            # Grok pattern names are valid! :)
            pattern => "^%{YEAR}-%{MONTHNUM}-%{MONTHDAY} %{TIME},%{INT} \[%{INT:Thread}\] %{WORD:LogType}"
            negate => true
            what => previous
        }
    }
    file {
        type => "nomination:msmq:group"
        tags => [ "nomination.msmq.logs" ]
        path => "/\/\US-COL-SVV-0598/\c$/\logs/\dh.Messaging/\Nominations/\Group.log*"
        add_field => { 
            "server" => "systest" 
            "market" => "krogerus" 
        }
        codec => multiline {
            # Grok pattern names are valid! :)
            pattern => "^%{YEAR}-%{MONTHNUM}-%{MONTHDAY} %{TIME},%{INT} \[%{INT:Thread}\] %{WORD:LogType}"
            negate => true
            what => previous
        }
    }
    file {
        type => "nomination:msmq:emailer"
        tags => [ "nomination.msmq.logs" ]
        path => "/\/\US-COL-SVV-0598/\c$/\logs/\dh.Messaging/\Nominations/\Emailer.log*"
        add_field => { 
            "server" => "systest" 
            "market" => "krogerus" 
        }
        codec => multiline {
            # Grok pattern names are valid! :)
            pattern => "^%{YEAR}-%{MONTHNUM}-%{MONTHDAY} %{TIME},%{INT} \[%{INT:Thread}\] %{WORD:LogType}"
            negate => true
            what => previous
        }
    }
}
filter {
    if "nomination.msmq.logs" in [tags] {
        grok { 
            match => { "message" => "%{TIMESTAMP_ISO8601:a_timestamp} \[%{INT:a_thread}\] %{WORD:a_logtype} %{GREEDYDATA:a_service} %{SYSLOG5424SD:a_processname} %{SYSLOG5424SD:a_bulkuploadid} - %{GREEDYDATA:a_message}" }
        }
        date {
            match => [ "a_timestamp", "yyyy-MM-dd HH:mm:ss,SSS" ]
            target => "@timestamp"
        }
    }
}